<% posts_link = link_to 'All Posts', posts_path %>

<%= render 'shared/content_title', title: "#{posts_link} &raquo; All Categories".html_safe %>

<% @categories.each do |category| %>
<div class='well'>
<%= category.name %>
</div>

<%end%>


